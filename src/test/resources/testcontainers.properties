# Never try to pull images
testcontainers.image.pull.policy=never

# Disable Ryuk completely
testcontainers.ryuk.disabled=true

# Allow container reuse (important for stability)
testcontainers.reuse.enable=true